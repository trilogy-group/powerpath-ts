/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { powerPathLessonPlansDeleteLessonPlanItem } from "../../funcs/powerPathLessonPlansDeleteLessonPlanItem.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteLessonPlanItemRequest$inboundSchema,
};

export const tool$powerPathLessonPlansDeleteLessonPlanItem: ToolDefinition<
  typeof args
> = {
  name: "power-path-lesson-plans-delete-lesson-plan-item",
  description: `Delete a lesson plan item


    Deletes a lesson plan item by its ID.

    This will delete the lesson plan item and all of its associated lesson plan items.
    WARNING: This operation is irreversible.
    `,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await powerPathLessonPlansDeleteLessonPlanItem(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
