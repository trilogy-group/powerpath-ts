/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { lessonsGetProgress } from "../../funcs/lessonsGetProgress.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAssessmentProgressRequest$inboundSchema,
};

export const tool$lessonsGetProgress: ToolDefinition<typeof args> = {
  name: "lessons-get-progress",
  description: `Get assessment progress

Returns the progress the student has made in the given PowerPath lesson`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await lessonsGetProgress(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
